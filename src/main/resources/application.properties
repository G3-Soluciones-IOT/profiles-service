spring.application.name=profiles-service
server.port=${SERVER_PORT:8082}

spring.config.import=optional:configserver:${CONFIG_SERVER_URI:http://config-service:8888}
spring.cloud.config.retry.max-attempts=20
spring.cloud.config.retry.initial-interval=1000
spring.cloud.config.retry.max-interval=3000

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DATABASE:profiles_db}}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:${POSTGRES_USER:postgres}}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:${POSTGRES_PASSWORD:password}}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.postgresql.Driver}
spring.jpa.database-platform=${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.open-in-view=false

eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://eureka-service:8761/eureka/}
eureka.instance.prefer-ip-address=false
eureka.instance.hostname=${EUREKA_INSTANCE_HOSTNAME:profiles-service}
eureka.instance.instance-id=${spring.application.name}:${server.port}

authorization.internal-service.secret=${INTERNAL_SERVICE_SECRET:internal-service-secret-key}
authorization.jwt.issuer=${JWT_ISSUER:iam-service}
authorization.jwt.jwks-uri=${JWT_JWKS_URI:http://iam-service:8081/api/v1/jwks/.well-known/jwks.json}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${authorization.jwt.jwks-uri}

profiles.integrations.iam.base-url=${IAM_SERVICE_BASE_URL:http://iam-service}
profiles.integrations.recipes.base-url=${RECIPES_SERVICE_BASE_URL:http://recipes-service}
